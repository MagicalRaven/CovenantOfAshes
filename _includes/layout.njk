<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header>
    <h1><a href="/">Covenant of Ashes</a></h1>
    <nav>
      <ul>
        <li><a href="/">Inicio</a></li>
        {% for page in collections.actos %}
          <li><a href="{{ page.url }}">{{ page.data.title }}</a></li>
        {% endfor %}
      </ul>
    </nav>
  </header>

  <main>
    {{ content | safe }}
  </main>
</body>
</html>
